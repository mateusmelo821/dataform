config {
    type: "table",
    name: "members_table"

}

SELECT
  *
FROM
  SCS.MEMBERSHIP_REGISTRATION
WHERE
  DATACRIACAO >= '2024-01-01' QUALIFY ROW_NUMBER() OVER (PARTITION BY CPF ORDER BY DATACRIACAO) = 1
LIMIT 1000